const SectionTitle = ({ text }) => {
  return (
    <div className="p-5 m-2 text-brown-800">
      <h2 className="font-medium tracking-wide">{text}</h2>
    </div>
  );
};

export default SectionTitle;
