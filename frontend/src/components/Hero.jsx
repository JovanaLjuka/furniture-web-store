import { Link } from 'react-router-dom';
import { Collections, Slider } from '../components';

const Hero = () => {
  return (
    <>
      <Slider />
      <Collections />
    </>
  );
};

export default Hero;
