import { Collections, Slider } from '../components';

const Hero = () => {
  return (
    <div className="w-full h-full">
      <Slider />
      <Collections />
    </div>
  );
};

export default Hero;
